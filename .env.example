# SQLite database (created automatically on first migrate).
DATABASE_URL="file:./data/port.db"

# Optional: required to clear the shared whiteboard via API.
ADMIN_SECRET="change-me-to-a-long-random-string"
